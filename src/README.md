# Source Code

Project code lives under `src/us_housing`.

The current Streamlit application remains at the repository root as `streamlit_app.py`, while reusable helpers and future pipeline code should go into the package modules:

- `data`
- `model`
- `inference`
- `pipelines`
- `utils`
- `visualization`

